name: Build & Test

on:
  pull_request:
    branches: [ main ]
    paths:
      - src/Nox.Reference.Abstractions/**
      - src/Nox.Reference.Holidays/**
      - .github/workflows/nox.reference.holidays.ci.yaml
  push:
    branches: [ main ]
    paths:
      - src/Nox.Reference.Abstractions/**
      - src/Nox.Reference.Holidays/**
      - .github/workflows/nox.reference.holidays.ci.yaml

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: ['6.0.x']

    steps:
      - uses: actions/checkout@v3
      - name: Setup .NET Core SDK ${{ matrix.dotnet-version }}
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: ${{ matrix.dotnet-version }}
      - name: Restore
        run: dotnet restore ./src/Nox.Reference.Holidays/Nox.Reference.Holidays.csproj
      - name: Build
        run: dotnet build ./src/Nox.Reference.Holidays/Nox.Reference.Holidays.csproj --warnaserror --configuration Release --no-restore      